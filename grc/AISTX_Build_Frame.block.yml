# auto-generated by grc.converter

id: AISTX_Build_Frame
label: AIS Frame Builder
category: AISTX

parameters:
-   id: repeat
    label: Repeat
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: enable_NRZI
    label: Enable_NRZI_Conversion
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: tag
    label: Length tag name
    dtype: string
    default: packet_len

inputs:
-   domain: message
    id: sentence
    optional: true

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import AISTX
    make: AISTX.Build_Frame(${repeat}, ${enable_NRZI}, ${tag})

file_format: 1
